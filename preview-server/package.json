{
	"name": "preview-server",
	"private": "true",
	"version": "1.0.0",
	"description": "live-preview-server",
	"author": "anilkumarum",
	"license": "GPL-3.0-only",
	"main": "server.mjs",
	"type": "module",
	"scripts": {
		"test": "node --test",
		"dev": "node --watch server.mjs",
		"bundle": "esbuild ./server.mjs --allow-overwrite --bundle --minify --splitting --outdir=out --format=esm --platform=node"
	}
}